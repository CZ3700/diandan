import type { ReactNode } from "react";

import "@fan-support/design-tokens/fonts/vietnamese.css";

export default function VietnameseFoundationLayout({
  children,
}: Readonly<{ children: ReactNode }>): ReactNode {
  return children;
}
