import type { ReactNode } from "react";

import "@fan-support/design-tokens/fonts/simplified-chinese.css";

export default function SimplifiedChineseFoundationLayout({
  children,
}: Readonly<{ children: ReactNode }>): ReactNode {
  return children;
}
