{
  "name": "@fan-support/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run --config ../../vitest.config.ts --root . --coverage",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@fan-support/contracts": "workspace:*"
  },
  "devDependencies": {
    "fast-check": "4.9.0"
  }
}
