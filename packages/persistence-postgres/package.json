{
  "name": "@fan-support/persistence-postgres",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:with-dependencies": "pnpm turbo run build --filter @fan-support/persistence-postgres --concurrency=1",
    "migrations:catalog": "pnpm run build:with-dependencies && node ./scripts/migration-manifest.mjs --check && node ./scripts/postgres-integration.mjs --write-catalog",
    "migrations:manifest": "pnpm run build:with-dependencies && node ./scripts/migration-manifest.mjs",
    "migrations:manifest:check": "pnpm run build:with-dependencies && node ./scripts/migration-manifest.mjs --check",
    "test": "vitest run --config ../../vitest.config.ts --root .",
    "test:postgres": "pnpm run build:with-dependencies && node ./scripts/migration-manifest.mjs --check && node ./scripts/postgres-integration.mjs && node ./scripts/postgres-key-version-upgrade.mjs && node ./scripts/postgres-media-object-key-upgrade.mjs && node ./scripts/postgres-outbox-status-upgrade.mjs && node ./scripts/postgres-constraints.mjs && node ./scripts/postgres-publication-upgrade.mjs && node ./scripts/postgres-repositories.mjs",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@fan-support/persistence-port": "workspace:*",
    "drizzle-orm": "0.45.2",
    "pg": "8.23.0"
  },
  "devDependencies": {
    "@fan-support/contracts": "workspace:*",
    "@types/pg": "8.23.1"
  }
}
