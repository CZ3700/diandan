P0-05 local OCI observability evidence
Captured: 2026-09-03T04:58:49+08:00 to 2026-09-03T05:20:05+08:00
Host Docker: 2 CPUs, 4095107072 bytes memory, overlayfs, Compose 5.1.3
Host TLS prerequisite: OpenSSL 3.6.3

Commands:
  mise exec node@24.20.0 -- corepack pnpm preview:up
  mise exec node@24.20.0 -- corepack pnpm preview:verify

Result: PASS after three harness-only corrections
  The first preview:up built and started all services healthy, then exposed a
  case-sensitive Docker "no such object" matcher in probe cleanup. The matcher
  was corrected without changing service code. Replacing a fixed injection delay
  with a health-driven trigger then exposed a missing readiness retry; the probe
  now polls within a fixed deadline. Independent review then found that the Next
  failure probe read only stdout; it now combines stdout and stderr and fails on
  an unsuccessful log read. Final preview:verify and preview:up pass.

Verified behavior:
  Storefront/API correlation: caller request ID and W3C trace ID were preserved;
    child spans were distinct and all structured fields were allowlisted.
  Privacy: authorization, cookie, query, startup and runtime failure canaries did
    not appear in responses or captured application stdout/stderr.
  API startup failure: exit 1, signal null, exactly one STARTUP_FAILED record.
  API fatal failure: exit 1, signal null, exactly one FATAL_RUNTIME_ERROR and one
    runtime.stopped record after three detached rejections.
  Storefront Next failure: three fixed next.runtime.failed records, health stayed
    available, SIGTERM exit 143, one runtime.stopped and no stop_failed record.
  Admin Next failure: three fixed next.runtime.failed records, health stayed
    available, SIGINT exit 130, one runtime.stopped and no stop_failed record.
  Worker: explicit SIGTERM exit 0, exactly one runtime.stopped record, then a fresh
    healthy process with exactly one runtime.started record.
  PostgreSQL query: 180006:1.
  Object-storage TLS: signed bucket PUT/HEAD and API/worker internal CA probes pass.
  TLS cache: exactly one active managed directory after start/verify; stale
    directories are removed and launcher assertions fail closed on mismatch.

Independent host standalone check:
  Fresh storefront/admin standalone builds repeated the three-rejection canary
  probes, post-failure health checks, SIGTERM/SIGINT exit-code checks, unique
  runtime.stopped checks, port closure, and exact child-PID cleanup. All passed.

Scope: this proves a local ephemeral preview only. PostgreSQL and object-storage
data use tmpfs. No cloud exporter, staging, production, persistence, backup,
multi-architecture, registry-published digest, or release attestation is proven.
